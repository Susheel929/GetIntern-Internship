# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Node (in case tooling like `serve` or `npx` is used locally)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
yarn.lock

# Logs
*.log

# Local env files
.env
.env.local

# Build/temp output (not used by this static project, but ignored defensively)
dist/
build/
.tmp/
.cache/
